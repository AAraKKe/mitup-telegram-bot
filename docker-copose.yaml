services: 
  run-tests:
    build:
      dockerfile: ./dev/docker/Dockerfile.dev
    volumes:
      - .:/bot
    command: hatch run dev:test

  postgres:
    image: postgres:12.1
    restart: always
    ports:
      - 5432:5432
    volumes:
      - ./postgres-data:/var/lib/postgresql/data
